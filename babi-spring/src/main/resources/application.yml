server:
  port: 8902

spring:
  application:
    name: babi-spring

babi:
  agent:
    workspace: ~/babi-workspace
  dashscope:
    api-key: ${DASHSCOPE_API_KEY:}
    chat:
      model: qwen-plus
      temperature: 0.7
      top-p: 0.9
      enable-search: false
