server:
  port: 8900

spring:
  application:
    name: babi-agent

babi:
  agent:
    workspace: ~/babi-workspace

agentscope:
  model:
    api-key: ${DASHSCOPE_API_KEY:}
    name: qwen-plus
    fallback: qwen3.7-max
